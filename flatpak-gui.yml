app-id: cz.tttie.KiOS.UI
branch: "master"
build-extension: true
runtime: cz.tttie.KiOS
runtime-version: 'master'
sdk: org.kde.Sdk//6.3
modules:
  - name: app
    buildsystem: simple
    build-commands:
        - cp -a . /app/gui
    sources:
      - type: dir
        path: gui

